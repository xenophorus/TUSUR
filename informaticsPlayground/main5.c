#include <stdio.h>
#include <stdlib.h>

int main5(int argc, char *argv[]) {

    int m, x, k;

    printf("%lu\n", sizeof(float) * 9 + sizeof(double) * 7);
    printf("%lu\n", sizeof(float));
    printf("%lu\n", sizeof(double));

    if (x<12){m=12; k=1;} else {m=10;k=0;}

    return 0;
}